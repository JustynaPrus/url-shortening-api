import styled from "styled-components";

export const StyledNav = styled.main`
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100vw;
  padding: 10px 20px;
  ul {
    list-style: none;
  }
  .hamburger {
    cursor: pointer;
    height: 30px;
    width: auto;
  }
`;
